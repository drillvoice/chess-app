// Minimal test component to isolate the useRef issue
function TestApp() {
  return (
    <div>
      <h1>Test App</h1>
      <p>If you see this, React is working properly</p>
    </div>
  );
}

export default TestApp;